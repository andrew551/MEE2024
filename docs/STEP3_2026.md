# Step 3 — the eclipse-field reduction: running record

The operational contract is [`STEP3_PLAN.md`](STEP3_PLAN.md); the campaign reasoning behind
it is [`REFRACTION_2026.md`](REFRACTION_2026.md). This file records what the reduction
itself finds, in the style of the campaign record: numbers with units, supersessions marked,
mistakes kept where they taught something.

Canonical inputs, restated once (provenance in the plan): plate scale **2.2054043 ″/px**
(CAL_piLeo, 16 frames, six 08-12 references, HC3-class ~25 ppm, `observation_time
18:29:35`); zenith cubic from the six 08-12 files, d(3000) = 3.0297 ″; totality air
29.2 ± 0.7 °C, 896.7 hPa, RH 0.208, λ 0.62 µm; C2 ≈ 18:28:07, C3 = 18:29:53.9 UTC;
R⊙ = 947.1 ″. Analysis source `G:\Leon Aug 2026` only.

---

## S0 — the SCI_ladder, measured (2026-08-29)

### The ladder itself

**87 frames spanning 39.2 s (18:28:12.9 → 18:28:52.1 UTC), a symmetric exposure ladder
0.1 → 0.3 → 0.6 → 1.2 → 0.6 → 0.3 → 0.1 s**, gain 0 (the night sets use 101), plus one
~50 ms default-setting frame in `discard/` (corner sky 525 ADU against the 0.1 s blocks'
826 — about 0.064 s equivalent). Integration per tier: 4.6 / 6.9 / 7.2 / 6.0 s — comparable
by design, at four saturation radii. The science sequence occupies the **first third of
totality**; CAL_piLeo (18:29:19–53) the last third; the ~27 s between them is the slew.

**The EXPTIME trap is confirmed on this dataset, five times.** Every frame whose header
disagrees with its folder has a corner-sky level matching its **folder**, not its header
(e.g. `0.1s/18:28:21.008`, header 0.30 s: sky 805 ADU against the 0.1 s block's 826 and the
0.3 s block's 1398). The folder organisation is the truth; the first frames after a SET
EXPOSURE change carry the new header on the old exposure.

**Saturation is entirely coronal and the 2 R⊙ inner cut survives every tier**: the clipped
region (99th-percentile radius of a mid-block frame) runs **1.38 / 1.57 / 1.71 / 1.86 R⊙**
for 0.1 / 0.3 / 0.6 / 1.2 s. No *star* comes near clipping — the brightest matched star
peaks at ~8 000 ADU in the 1.2 s frames against a 65 535 ceiling, so F16 has nothing to
reject on the science field at these exposures (it remains mandatory for CAL_piLeo and any
deeper future tier). The sky *falls* ~8 % through the science sequence (826 → 758 ADU at
0.1 s) — the approach to mid-totality; the 726 → 5700 ADU **rise** in the record belongs to
CAL_piLeo's half, approaching C3.

### The catalogue, projected onto the field

An empirical sky→pixel affine from the matched stars (5.2 px mean residual) puts the Sun at
RA 142.146° Dec +14.895° — 0.72° from frame centre, matching the recorded 0.74° — and
counts **150 catalogue stars G ≤ 11 on the sensor, 139 in 2–11 R⊙** (the record's 132, the
difference being edge effects). By annulus: 18 in 2–3 R⊙, 18 in 3–4, 38 in 4–6, 49 in 6–8,
16 in 8–11. The Sun-centre pixel used throughout S0 is the clipped-region centroid
(3110, 3204); the record's (3208, 3293) differs by 132 px = 0.31 R⊙ and the discrepancy is
an **open item** — it shifts every radius and therefore h.

### What detection reaches, and what limits it — three campaigns of stage 1

All runs: daytime centroid regime (`centroid_gaussian_subtract=True`, thresh 4.0,
`min_area=2`), per-tier stacks (the folder is the exposure truth), stage 2 importing the
zenith cubic at `distortion_fixed_coefficients=quadratic`, corrections ON, per-tier
exposure-weighted mid-times, `distortion_fit_tol 2.0` (the M3-validated diagnostic value —
the final `tol 999` waits on F16-per-frame and robust per-star aggregation, per the plan).

**Raw frames, wide blob margins** (hull + 650 px — inherited, see the trap below): three
tiers solve, 47-star union, V ≤ 10.48, **nothing inside 2.64 R⊙**, 2 of the 36 catalogue
stars in 2–4 R⊙, **h = 33.0 R⊙²** against the design's 19.8. δL/L scales with h: a 1.67×
sensitivity penalty concentrated exactly where the 1/r signal is strongest.

**Raw frames, default margins** (hull + 130 px): the deep tiers improve, but the 0.1 s tier
**fails to platesolve** — its smaller saturated hull exposes the steep unsaturated rim, 107
of 160 detections land in the 1.5–2.0 R⊙ ring, and the solver feeds on them
brightest-first. **The blob margin is a knob with no good setting**: wide walls off the
inner annulus, tight feeds the solver junk, and the shallowest tier — the one that reaches
deepest — is hurt worst.

**Bruns-style coronal subtraction** (`tools/step3_s0_blursub.py`, prototyped from the 2017
paper's ECLIPSE pre-step): per tier, the unshifted mean of the raw frames is blurred with a
10 px Gaussian — hiding the stars, keeping the coronal shape — and subtracted from every
frame (2000 ADU pedestal; raw-saturated pixels re-imposed at 65 535 so the blob machinery
still masks the core). Three iterations, each of which taught something:

1. Tight margins (hull + 40 px) everywhere: deep tiers solve for the first time at such
   margins; shallow tiers get *worse* than raw (1190 detections at 0.1 s, 1146 inside
   2 R⊙).
2. Registering the model to each frame's corona by phase correlation (the mount drifted
   2.04 px between the 0.1 s tier's two blocks): **changed nothing, to the digit.** The
   residual is not a displacement error.
3. The subtracted stack's own statistics located the real obstacle: the field is flat to
   **4–8 ADU beyond 2.2 R⊙** — the subtraction works — but a band at 1.2–1.8 R⊙ carries
   110–264 ADU of **coronal fine structure**: streamer rays and the clip-boundary kink, at
   star scale, the same filaments visible in Bruns' fig 8(c)/(d). No smooth background
   model removes structure at star scale. **~2 R⊙ is a physical inner limit for this
   corona, not a parameter choice — and it coincides with the design's inner cut.** With
   per-tier masks sized to put the exclusion at ~2.0 R⊙ (margins 200/150/90/40 px), **all
   four tiers solve**.

### The S0 result

| | raw, wide margins | **blur-subtracted, 2 R⊙ masks** | design field |
|---|---|---|---|
| tiers solving | 3 of 4 | **4 of 4** | — |
| union of matched stars | 47 | **49** | 132 (G ≤ 11) |
| V range | 7.13–10.48 | 7.13–10.48 | — |
| innermost star | 2.64 R⊙ | **1.93 R⊙** | 2.0 R⊙ cut |
| 2–3 R⊙ (18 in catalogue) | 1 | **4** | — |
| 3–4 R⊙ (18 in catalogue) | 1 | 1 | — |
| **h = 1/mean(1/r²)** | **33.0 R⊙²** | **22.4 R⊙²** | **19.8 R⊙²** |

Per-tier plate scales agree with each other to ~50 ppm and with CAL_piLeo to well inside
100 ppm (constant+linear+quadratic free, different star sets — a consistency check, not a
measurement). Beyond detection, the subtraction also removes the coronal-gradient centroid
bias — Bruns measured up to 0.1 ″ of skew from the raw gradient — for **every** star, which
is an error-budget improvement even where detection was never in doubt.

### Traps recorded (each cost this session real time)

- **The CLI loads the interactive `MEE_config.txt` underneath its `--set` overrides.** The
  first S0 run silently inherited `blob_radius_extra=500 / centroid_gap_blob=150` from a
  previous classic-UI session — a 650 px exclusion that walled off 2–4 R⊙ and was initially
  misread as a detection-physics problem. Pin *every* stage-1 parameter in a driver.
- **Killing a driver script does not kill its running pipeline child.** An orphaned stage-1
  subprocess survived a kill, wrote a blob-off archive into a freshly-cleaned tree, and the
  resumable driver adopted it — a 1.2 s "failure" that was really a poisoned input. Check
  for orphans before relaunching into the same output tree.
- `delete_saturated_blob=True` is mandatory on this field (the CAL_piLeo recipe has it off
  — that field has no Sun in it). With it off, the coronal rim supplies hundreds of
  star-like detections and the solver dies.

### S0 addendum — V4: the forbidden region, solve inheritance, and an honest floor
(2026-08-29, after Douglas' challenges; supersedes three claims above)

**The "~2 R⊙ physical inner limit" above is WITHDRAWN as stated.** Douglas challenged it
twice and both challenges measured out. The 1.2–1.5 R⊙ σ = 264 ADU was inflated by the
blob's own darkening step; the honest MAD on un-blobbed subtracted frames is **75 ADU at
1.5–1.7 R⊙, 32 at 1.7–1.9, 17 at 1.9–2.1**. And the MEE reduction of Bruns 2017 matched
V 7.78 at 1.49 R⊙ and V 7.15 at 1.63 — two bright stars inside 2 R⊙, then nothing until
V 10.57 at 2.08. The floor is **brightness-dependent**, not a wall, and 2017 was never
better in kind: it had bright stars close in, and V3's mask never let Leon try.

**The Sun centre is (3171, 3232)** — the astropy ephemeris through the fitted cubic. The
clipped-region centroid used above (3110, 3204) is 67 px off (streamers bias it); the
plan's (3208, 3293) is 71 px off the other way. All S0 radii above carry that ~0.15 R⊙
uncertainty; V4 onward uses the ephemeris value.

**V4** (`tools/step3_s0_v4.py`, outputs `step3_s0_v4/`) drops the convex-hull blob
entirely — Douglas' design: a Sun-centred **forbidden disk** painted flat at the pedestal,
radius max(1.25 R⊙, the tier's measured saturation radius + 20 px) = 1.47/1.63/1.76/1.91
R⊙ per tier, residual saturated streamer tips dilated 10 px and painted too. Detection is
otherwise free. Results: the deep tiers solve (0.6 s: 257 centroids → 38 matched; 1.2 s:
101 → 26, rms 0.517 ″ — the best of any S0 run); the shallow tiers' *plate solves* die
(673 and 474 centroids — fake stars near the disk edge dominate the brightest-N list the
solver feeds on). Scored offline through the deep tier's solution (a cubic sky→px fit from
45 pipeline-matched anchors, 0.90 px residual — a LINEAR affine leaves 5.3 px on this
field, the refraction compression, and silently drops matches), the fakes cost nothing:
**detection is filtered by catalogue matching, the second and more rigorous filter**
(Douglas' framing). The fake-star problem is a *solver* problem only, and the solver fix
is inheritance — the shallow tiers taking the deep tier's solution, which the pipeline
cannot yet do (logged with F21/F22 as the eclipse case).

**The vetting doctrine, demonstrated with a casualty.** The offline matcher initially
reported V 8.2 at 1.50 R⊙ — Leon's Bruns-analogue. The PSF check killed it: a flat 5×5
core, SNR 2.3, a fine-structure clump 1.85 px from the catalogue position, in the band
where fake density makes chance matches likely. **That fake would have carried more 1/r²
leverage than any real star in the field.** The plan's MAD-clipped per-star medians and
multi-tier consistency are not optional hygiene; this is the measured reason. Vetted
status of the inner candidates:

| candidate | evidence | verdict |
|---|---|---|
| V 9.1 @ 2.29 R⊙, V 8.2 @ 2.36 | pipeline-matched in BOTH deep tiers | **solid** |
| V 11.1 @ 1.92, V 13.0 @ 1.99, V 12.1 @ 2.06 | pipeline 1.2 s only; expected peaks ≲ local MAD | fake-matches (tol 2.0 against a mag-13 catalogue at the disk edge) |
| G 8.64 @ 1.94 | SNR 3.4 / 3.5 / 2.0 at the same spot in three tiers (combined ≈ 5.3) | promising; S2's catalogue-level merge decides |
| V 8.2 @ 1.50 | flat core, SNR 2.3 | **fake — the cautionary tale** |

**The number that matters**: h ≈ **21 R⊙² with vetted stars only** (the day began at
33.0; the design assumed 19.8), and 15–17 in reach if the 1.9–2.1 R⊙ band confirms at
combined depth. The uncurated 71-star union's h = 15.3 is recorded as what naive matching
would claim — the gap between 15.3 and 21 is the leverage of fakes.

**The discard frame: a clean null for 2027.** 965 detections, zero catalogue matches at
~50 ms, gain 0 — below the useful floor for this rig (and the control for the chance-match
rate: expected ~0.4, observed 0). The all-87 depth stack (24.7 s; an unweighted mean of
mixed exposures is photon-optimal since matched-filter weights are exposure/exposure)
also needs the inherited solve; its 23 offline matches await proper scoring.

**The four-dataset validation matrix** (Douglas). The same tools with the same defaults
must reduce: 2017 Wyoming (`I:\2017 eclipse images Don Bruns\...`, the MEE re-analysis
stacked all 45 frames at once, its alignment leaning on two bright inner stars opposite
the Sun), 2024 Mexico (**`G:\Mexico April 2024\Station-1-Eclipse-Data`** — the G: copy,
not I: — pure Method 2, quintic, usability triage frame by frame), 2026 Leon, and the
2026 Portland full-moon set (`J:\Eclipse data\Toby Portland data\2026-07-29`, same rig as
Leon; the Moon-centric solver must return **L = 0** — the null method check). Minimal
hand tuning is the acceptance criterion and the defence against the tuned-analysis
accusation. The convex-hull blob has no remaining constituency — moon fields can use the
same forbidden-disk-plus-gradient treatment — and retires when this matrix passes.

### S0 addendum 3 — the scoreboard after Douglas' questions (2026-08-29)

**Tiers are not independent witnesses inside ~2 R⊙** — a correction to the addendum above.
The coronal fine structure is quasi-static across the 39 s ladder, so a clump that appears
at the same position in three tiers proves the *corona* persists, not that a star exists.
The "combined SNR ≈ 5.3" argument for G 8.64 @ 1.94 is therefore withdrawn; only PSF
shape, sub-pixel positional agreement, and photon-independent checks discriminate there.

**V 9.1 @ 2.29 demoted** (Douglas asked for its positional errors, and they answer him):
match error **2.725 ″ in the 0.6 s tier and 1.167 ″ in 1.2 s, outlier-flagged in both**.
Whether a degraded real star or a fake, the robust aggregation would clip it; it is not an
anchor. **V 8.2 @ 2.36 stands** (0.329 ″ clean in 0.6 s; 0.634 ″ in 1.2 s).

**The below-Sun star is real, and it is the best inner anchor in the field.** G 7.71,
detected at (3161, 4163) in the 0.3 / 0.6 / 1.2 s tiers within 1.4 px of each other —
**area 6 px (a genuine PSF, against the fakes' 2), SNR 8** on the 1.2 s stack. Radius
**2.17 R⊙, on the opposite side of the Sun from the rest of the field** — the geometry
2017's success leaned on (its two bright inner stars sat opposite the Sun), which breaks
the translation–deflection coupling no same-side star can. It was never in any matched
list because it sits **13 px from the bottom frame edge**: `remove_edgy_centroids`
discards it, and the mount's pointing error is what put it on-frame at all. It must be
deliberately admitted at S2 (with the edge-distortion caveat priced), and the 2027 design
note writes itself: offset the field so bright stars land on *both* sides of the Sun on
purpose, not by accident. With it, vetted h ≈ **20.0 R⊙²** — at the catalogue-complete
19.8 within its own uncertainty.

**Darks exist for exactly the science tiers** — `G:\MEE_output\Library 1` holds
`dark_ASI2600MM_g0_{0p100,0p300,0p600,1p200}s_bin1`. Douglas' proposal adopted: the
master-dark hot-pixel map becomes a vetting layer (integer-shift stacking with ~2 px
drift keeps hot clusters compact, so they mimic stars; today's *coronal* fakes are
photonic and darks cannot catch those, but the hot class they do catch is exactly the
kind an individual-centroid regime cannot afford). Flat-field dust-donut bias is
priceable with the §13 sky-gradient formula applied to the flat's local gradients —
queued behind confirming a flat library exists for this rig.

### The first preliminary L (2026-08-29) — the chain closes, the value does not yet

`tools/step3_prelim_L.py`: the full step-3 chain — zenith cubic → CAL_piLeo canonical
(2.2054043 ″/px imported) → constant-only fit on the V4 subtracted science stacks,
corrections ON — then stage 3, Methods 1 and 2, `eclipse_limiting_mag 11`:

| tier | stars | **Method 1 L (″)** | printed σ (″) | honest σ (″) | Method 2 L (″) | M2 scale vs CAL |
|---|---|---|---|---|---|---|
| 0.6 s | 37, rms 0.824 ″ | **2.81** | 0.57 | ~0.76 | 3.23 | −16 ppm |
| 1.2 s | 23, rms 1.091 ″ | **3.14** | 0.57 | ~0.76 | 3.38 | −9 ppm |

GR predicts 1.751 ″. The two tiers (not independent — shared stars, shared atmosphere)
sit **+1.1 to +1.4 ″ high, 1.4–1.8 σ by the honest error** — the printed σ understates the
eq.-23 term by the plate scale (the PR #6 bug; the honest column adds it back by hand at
δS = 25 ppm × h). **This is a chain-closure milestone, not a measurement**: no per-star
MAD medians, no F16-per-frame, the below-Sun anchor still edge-trimmed out, tol 2.0
(mismatch-safe but able to clip large deflection excursions), and above all **no S1
nuisance estimator** — the M5 rehearsal produced δL of −0.97 to +0.41 ″ on night fields
with *zero* true deflection, which is exactly the class of atmospheric inheritance sitting
uncorrected in these numbers. The diagnostics behave as designed: the Method-1/Method-2
gap (0.24–0.42 ″) and Method 2's fitted scale sitting 9–16 ppm below the imported one are
the §5.4 error meters, both reading nonzero as the campaign record predicts they must.

### Why the below-Sun anchor is not yet in any fit — mechanism found (2026-08-29)

Three suspects were eliminated by measurement: not the edge trim (the code keeps points
3–16 px from the edge, and the star is in every detection list — an earlier claim here was
wrong), not the double filter (`is_double(10″) = False`), not the fit tolerance (absent at
tol 3, 5, and 999 alike). The measured fact: **no star below py = 3825 ever matches**, at
any tolerance. The cause is in `match_centroids`: candidate association projects
detections through the **rough linear solve**, whose error on this refraction-compressed
field grows toward the frame edge and crosses the 36 ″ `rough_match_threshhold` at about
that line. Raising the threshold to 80 ″ is not a fix — the nearest-neighbour association
then mismatches wholesale (pre-outlier rms 21.4 ″, 43 of 44 candidates rejected, fit
collapsed). The proper fix is the matcher's own TODO: associate with the **fitted** model
(two-pass: fit on safely-matched stars, re-match the remainder through the result), which
is the S2 tool and the F21/F22-family pipeline item. Until then the anchor enters by
manual join — its pixel position is measured in every tier and its catalogue identity is
unambiguous.

Bonus finding from the same hunt: **"V 9.0 @ 1.87" is a flagged double** (companion
within 10 ″) — the pipeline excludes it from matching by design, closing that question.
Three different exclusion mechanisms in one field: a fake, a double, and an
edge-of-rough-model casualty.

**Dark library verdict (Douglas' doubt resolved): correct and usable.** All six gain-0
masters: median 502.4 ADU against recorded bias 500.9 (dark current negligible, as it
must be at gain 0, +10 °C, ≤ 2 s), MAD 0.56 ADU, hot-pixel counts 5 972 → 6 592 growing
monotonically with exposure, CCD-TEMP 9.6–12.4 °C against the science setpoint 10 °C,
same camera ID, 49–50 frames each, `mee2024-callib` format v1 — and `calibration.py` is
unchanged since the v1.3.7 line, so the format is current. The hot-pixel vetting layer
builds on these as they stand.

### The excess was mostly unfiltered outliers: L moves to 1.7–2.1 ″ (2026-08-29, late)

Douglas asked what was happening with the table's rank-1 star, and the answer restructured
the preliminary L. The outlier criterion is `error ≥ distortion_fit_tol`, applied during
fit iteration (a flagged star keeps its flag while the CSV's `error(")` is re-measured
against the final refitted model — which is how a star can show 1.69 ″ with a flag at
tol 2.0). Rank 1 (V 9.10 @ 2.29 R⊙) is neither stale (`missing_pm = False`) nor double:
its centroid is genuinely displaced 2.2–3.1 ″ in every reduction — corrupted, presumably
by structure. And the load-bearing discovery: **stage 3 reads
`CATALOGUE_MATCHED_ERRORS.csv` and filters only `flag_is_double` — `flag_is_outlier` is
ignored.** The mag-11 cut caught the faint fakes, but V 9.10 entered the preliminary L
unfiltered, carrying the field's largest single weight (12.5 %).

Rerunning stage 3 on archives with flagged rows removed:

| tier | L(M1), flagged in | **L(M1), flagged out** | L(M2) | M1–M2 gap |
|---|---|---|---|---|
| 0.6 s (39→37 stars) | 2.81 ″ | **2.08 ″** | 2.10 ″ | 0.02 ″ |
| 1.2 s (40→23) | 3.14 ″ | **1.71 ″** | 1.91 ″ | 0.20 ″ |

GR: 1.751 ″. The Method-1/Method-2 gap collapsing from ~0.4 ″ to 0.02–0.2 ″ is the §5.4
diagnostic confirming that the removed error was real. Two caveats carried forward: the
1.2 s arm dropped 17 of 40 rows, so some genuine large-excursion stars likely went with
the junk (an attenuation risk, biasing low — the tol-999-plus-MAD regime at S2 is the
principled fix); and the atmospheric inheritance (±0.1–0.6 ″, S1's job) remains
unaddressed — the excess it explains is now the *residual* 0.0–0.35 ″, not the original
1.3 ″. **Pipeline item: stage 3 must respect `flag_is_outlier` (or robust-weight); that
is a results-changing change under ROADMAP §6 and needs its own entry.** Note also that
0.1 s and 0.3 s contributed nothing to any L yet — their solves fail pending the two-pass
rematch — so every number above rests on the 0.6 s and 1.2 s tiers alone.

### The two-pass rematch works, the anchor is in — and it measures the edge field
(2026-08-29, late; `tools/step3_rematch.py`)

The tool reconstructs the pipeline's own pixel→sky chain from a finished stage-2 archive
(`apply_corrections` → `linear_transform` → `to_polar`, coefficients and q from the zip)
and re-matches **every** detection against the AstroCorrect-corrected catalogue at the
observation epoch. Two things made it honest: a **round-trip gate** — the reconstruction
must reproduce the already-matched stars' stored RA/DEC(obs), and it does, to
**0.0000 ″** once the convention scan found the chain (polynomial in the original frame,
axis flip only at the rotation step) — and per-pass hygiene learned the hard way in three
iterations: catalogue **proper motions must be propagated to epoch** (unpropagated
positions are 1–2 ″ off — the reason this tool's own first pass missed the anchor; the
*pipeline* propagates pm correctly, and its failure remains the rough-linear boundary,
compounded by the anchor's ~2.4 ″ offset exceeding tol 2.0 — triply doomed in all), **doubles filtered**
(the first pass smuggled the known V 8.97 double past stage 3's only filter), and
**blends rejected** (two catalogue stars claiming one detection at px 1224, 64).

The sensitivity pair, on vetted (outlier-dropped) star sets:

| tier | vetted only | + pass-2 stars, no anchor | **+ anchor** |
|---|---|---|---|
| 0.6 s | 2.08 ″ | 2.33 ″ (41 stars) | **2.65 ″** |
| 1.2 s | 1.71 ″ | 1.87 ″ (25 stars) | **2.43 ″** |

**The anchor pulls L up by +0.33 / +0.56 ″.** Read correctly, this is a measurement, not
a disappointment: the anchor's total offset is 2.0–2.4 ″ against an expected deflection
of 0.81 ″ at 2.17 R⊙, so ~1.2–1.6 ″ of its displacement is *not* deflection — and it
sits at the frame's bottom edge, which is the **lowest-altitude sightline in the field**,
where the uncancelled refraction differential and any model-transfer edge error both
peak. The same pattern shows in the other pass-2 recoveries (all near frame edges,
separations 1.4–2.4 ″, all pulling L upward): **the field's edges are displaced outward
relative to the model — a smooth unmodelled field, which is precisely what S1's nuisance
estimator exists to absorb.** The anchor's leverage becomes usable when the nuisance
field can separate a smooth vertical/edge term from 1/r; until then its inclusion is a
sensitivity number, not a better L. The preliminary range stands at **1.7–2.7 ″
depending on star admission policy** — which is itself the cleanest statement yet of why
the estimator, not more stars, is the critical path.

## S1 — the nuisance estimator, built and gated (2026-08-29, late)

`tools/step3_s1_estimator.py`: one linear model over 2N residual components — [N1, N2,
Θ, L·(1/R)û] plus a smooth **vertical** nuisance field (sensor ŷ, which the CAL affine
puts 3.1° from the local vertical; M3 measured the wavefield vertically polarised at
V/H ≈ 2.3), amplitude a low-order 2D polynomial. Geometry and the base fit inherit from
`m5_projection.py`, whose basis recovers an injected L = 1.000 exactly.

**A methodological trap caught by its own too-good result.** The first gate projected the
night residuals through a degree-3 smoothing surface before the estimator saw them — and
a full *vector* degree-3 nuisance then produced nulls of exactly 0.0000″, because it
spans the surface's function space *by construction*. Circular; withdrawn. Gate v2 fits
the **un-smoothed per-star medians on the night field's own stars**, wavefield
patchiness included, with the bootstrap over per-star errors as the floor:

| variant | N1 null | N2 null | N3 null | worst (″) | floor (″) |
|---|---|---|---|---|---|
| base (Method 1) | −0.77 | −0.04 | +0.67 | 0.77 | 0.04–0.06 |
| **vertical deg-2** | **−0.19** | **+0.32** | **−0.19** | **0.32** | 0.03–0.06 |
| vector deg-2 | −0.75 | −0.15 | +0.57 | 0.75 | 0.07–0.17 |
| vector deg-3 | −0.49 | +0.83 | +0.63 | 0.83 | 0.10–0.22 |

Injected-L recovery is exact at every order (linear model — identifiability holds
throughout; criterion (b) passes trivially). **Criterion (a) fails: 0.32″ is ~6× the
floor. Per the plan's decision-before-looking rule, the gate FAILS and Leon's L is
quoted with the residual systematic stated.** What the estimator *does* earn: the
vertical deg-2 nuisance cuts the atmospheric inheritance **2.4× on all three real
atmospheres**, so the quoted systematic shrinks from the raw ±(0.1–0.97)″ class to
**±0.33″** with v-deg2 applied. The vector variants are *worse* than vertical-only on
real data — M3's polarisation measurement, vindicated in the estimator's own behaviour.

**Applied to Leon** (rematched, vetted tables; mag ≤ 11; stars outside 2 R⊙):

| set | N | L base (″) | **L with v-deg2 (″)** |
|---|---|---|---|
| 0.6 s, with anchor | 41 | 2.65 | **1.47** |
| 0.6 s, sans anchor | 40 | 2.30 | 1.19 |
| 1.2 s, with anchor | 25 | 2.44 | 2.85 |
| 1.2 s, sans anchor | 24 | 1.85 | 0.72 |

The 0.6 s set — the best-populated, and the one carrying the below-Sun anchor — lands at
**1.47″ against GR's 1.751**, inside the ±0.33″ systematic before statistics are even
counted; and the nuisance is doing exactly what the edge stars said it should (absorbing
the smooth outward edge field that pushed the raw fits high). The 1.2 s swings (+2.85 /
+0.72) are the honest cost: 24–25 stars cannot constrain 9 nuisance parameters, so the
estimator needs the S2 union — all tiers through the rematch machinery, 60–70 stars —
before its Leon number stabilises. The state of L at the end of S0+S1 day one:
**consistent with GR at roughly L ≈ 1.5–2.3 ± 0.33 (atmosphere) ± ~0.6 (statistics) ″**,
with every named systematic carrying a measured size and a designed next step.

## S2 first pass — the union: all four tiers, one table, one fit (2026-08-29, latest)

`tools/step3_s2_union.py`. The shallow tiers ride the 0.6 s fitted model with a per-tier
constant offset (drift ~2 px); per-tier median displacement subtracted so tiers mix;
per-star **median across tiers** with a cross-tier consistency vet (3× the field's
cross-tier MAD); doubles dropped at the catalogue; mag ≤ 11; R > 2 R⊙; match gate 4.5 ″
because it must exceed the largest *physical* displacement (the anchor's 2.4–2.9 ″ —
a 3 ″ gate silently rejected the most valuable star in the field). all87 is deliberately
not a member: same photons as the tiers, double-counting not depth.

**The vet works unsupervised**: it removed V 9.10 (cross-tier spread 1.83 ″ — the
corrupted centroid caught by hand earlier, now caught automatically) and G 8.95 at the
left edge (spread 2.04 ″). The previously unusable tiers now contribute: 24 stars from
0.1 s, 40 from 0.3 s.

| set | N | h (R⊙²) | L base (″) | **L v-deg2 (″)** | ± stat |
|---|---|---|---|---|---|
| per-tier alone (0.1/0.3/0.6/1.2) | 24/40/42/38 | — | — | 2.43 / 2.38 / 2.14 / 2.73 | — |
| **0.6+1.2, with anchor** | 42 | 27.1 | 2.88 | **1.98** | 0.60 |
| 0.6+1.2, sans anchor | 41 | 30.7 | 2.37 | 1.50 | 0.54 |
| full union, with anchor | 44 | 23.7 | 3.14 | 2.58 | 0.59 |
| full union, sans anchor | 43 | 26.1 | 2.92 | 2.26 | 0.58 |

Reading: the deep-tier union brackets GR (1.98/1.50 ± ~0.6); the full union sits higher
because the shallow tiers enter at equal per-star weight despite 3–7× less integration —
an unweighted median lets their noisier, more atmosphere-loaded displacements pull the
combined star positions. The known refinement is inverse-variance weighting across tiers
(and it is a *refinement*, not a rescue — the deep tiers carry most of the information
either way). Headline at the close of day one, S0 through S2 first pass:

**L(0.6+1.2 s union, anchor in, nuisance on) = 1.98 ± 0.60 (stat) ± 0.33 (atmosphere) ″**
— GR's 1.751 ″ well inside — with the full-union sensitivity (+0.6 ″) recorded as the
open weighting question, not hidden in an average.

### Open items for S1/S2

- **The 3–4 R⊙ annulus: 1 of 18 detected even after subtraction**, while 2–3 R⊙ improved
  to 4 — plausibly the luck of magnitudes in a small annulus (current stacks reach only
  V ≈ 10.5 on 4.6–7.2 s of integration), to be tested by a full-ladder combined stack
  (24.7 s) and catalogue-level tier merging at S2.
- **The Sun-centre discrepancy**: (3110, 3204) measured here vs (3208, 3293) in the record.
- The subtraction belongs in the pipeline eventually (results-changing, ROADMAP class) —
  the prototype and these numbers are its justification; until then it lives in
  `tools/step3_s0_blursub.py`.
- Whether a raw-frame run with masks at exactly 2 R⊙ (no subtraction) also solves — the
  clean control for how much of the gain is the subtraction and how much the mask geometry.
